# Rajesh's Learning Profile

Generated by learning-framework skill. Read by Stages 1 and 2; written by Stage 6.
Hand-editable.

Last updated: 2026-06-19

---

## Meta-notes on the framework itself (from v1's first run)

- 4-loop cadence felt right for "moderate complexity, standard budget" sessions
- Running calibration interval of K=3 was correct here — calibrated after Loop 3 just before synthesis
- The artifact-spec-drives-everything-backward principle held: declaring "3 day-templates" in Stage 1 was the right call initially, and the mid-session renegotiation to "skeleton-with-modules" was a real adaptation to his actual posture, not just a label change

---

## Domain: spark / data-engineering (entity resolution)

**Chunk size:** small–medium — one mechanism per loop (shuffle / joins / skew). Do NOT batch multiple strategies into one loop; each sub-concept needs its own worked treatment.

**Sustain window:** short and spiky. ~45-min budget declared but real energy oscillated: 3 → 3 → crashed to 1 after ~30 min → recovered to 3 after a pause → completed Loop 3. Honor energy-1 stops immediately (banked correctly, did not push into skew on the empty tank; he came back recharged). Expect a dip around the 30-min mark; a clean stop-offer at that point works well.

**Angles that worked:**
- ✅ **Worked tables, traced row-by-row** — by far the biggest lever. He explicitly asked twice for "take some tables and show me." Always draw input table → scatter across desks → shuffle → result. Tables > prose, every time.
- ✅ **Office/desks analogy** — partition=desk, worker=person, shuffle=carrying forms across the room. Landed instantly and he reused the vocabulary correctly in his own answers.
- ✅ **Mechanism-first, then the rule** — derive the rule from the worked example rather than asserting it.
- ✅ **Growth tables** (rows vs pairs) to make quadratic explosion visceral — the N²/2 table did the H/G work in one glance.

**Angles that didn't work / not tested:**
- ❌ **Terse multi-item intros** — compressing 3 join strategies into ~4 sentences each was too thin; he called it out directly. Under-teaching is his more common failure mode than over-teaching. (Now encoded in SKILL.md Stage 3b.)

**Prerequisites confirmed:**
- Strong on his own MDM/PySpark ER pipeline as anchor (PipelineEngine, StandardizationPlugin, blocking, candidate pairs).
- Solid: narrow vs shuffle, self-join condition, broadcast vs sort-merge.
- Gap exposed: had NOT seen self-joins before this session (got there fast once shown).
- Pattern: gets concepts right but drops the *labels* (named "skew" as its fix "better blocking key"; knew the recall trade-off but not the term "false negative"). At `use` depth, attach vocabulary explicitly after the concept lands.

**Known gaps (2026-06-16):**
- Skew fixes salting + isolate-hot-key covered at recognize-only, not drilled.
- Window functions, complex/nested types (#2), LSH/MinHash (#5), graph/connected-components (#6), string normalization (#3), perf (#7), pipeline design (#8) — all still ahead in this 8-topic arc.

**Sessions:**
- 2026-06-16 — Source: AI knowledge + pipeline context — Loops: shuffle → joins → skew (his H/G). Artifact: shuffle/join/skew decision-card. Depth `use`, passed (needed labels, not concept rebuilds). Note: he declined the Notion learning-log update this session.

**Notes:**
- 8-topic Spark/ER list is a 4–6 session arc. Entity Resolution (#4) is the declared spine; he chose bottom-up (mechanics first). Resume next session at: **complex/nested types (#2)** or **LSH (#5)**, OR drill salting if he wants skew-fixes hands-on.
- Bottom-up sequencing worked well — each mechanic paid off visibly into the ER problem he cares about.

---

## Module 01 — Complex types & nested data (2026-06-18)

**Covered (depth `use`):** Array (explode vs explode_outer, size/array_contains/filter, null-drop gotcha), Struct (dot access, struct() rebuild, fixed-fields contract), Map (key access, map_keys/values, explode-on-map, boolean value types, schema-evolution-free). Decision tree (Q1 fixed parts→struct, Q2 variable same-type→array, Q3 open-ended keys→map).

**Probe performance:** Strong. ~12/14 first-pass correct across loops. Syntax slips (withColumn first-arg must be plain string not col(); substring is 3-arg 1-indexed) but concepts solid. The two borderline scenarios (geo coord, boolean flags) initially wobbled then self-corrected.

**Key calibration insight:** He talks himself OUT of correct answers on borderline cases by over-weighting a salient-but-irrelevant feature (chose map for lat/lng because "both values are double"). Fix that worked: an explicit contrast table showing two cases with the SAME surface feature landing on different types, proving the feature isn't the deciding factor. The "X is a red herring; Y decides" framing lands well — use it whenever he's pattern-matching on the wrong signal.

**What worked:** Scenario-recognition drills (he explicitly requested "use cases to relate and absorb"). Real MDM data shapes (phones/address/signals) as the running examples. Decision tree as an ordered ask (Q1→Q2→Q3), not a flat list. Side-by-side same-feature-different-answer tables. Coding syntax is still weak — he reads/adapts from examples but can't yet type from scratch ("not yet able to type"); keep showing runnable snippets, don't assume recall.

**Known gaps:** null-handling inside arrays (Loop 5), schema evolution mechanics (Loop 6), array-of-structs (the real MDM shape — touched not drilled), transform/inline. These roll into a short follow-up before Module 02.

**Artifact:** nested-types-decision-card.html (interactive, matches Module 00's HTML walkthrough format).

**Sustain:** Solid full module across loops 1–4 at energy 3, no crash this time. Scenario-drilling re-energized him near the end rather than draining — recognition practice is a good "second wind" activity for him.

---

## Module 06 — Score Aggregation & Confidence (2026-06-19)
> *(Renumbered: was Module 05. Its code-fluency drills are now the standalone **Module 05 — Code Companion**, `score-aggregation-confidence-code.html`.)*

**Covered (depth `use`):** Why fixed thresholds fail (distribution problem), labeled data (stratified sampling, sizing), confusion matrix (TP/FP/FN/TN), precision ("how clean are merges") and recall ("how many matches caught"), PR curve (sweep + elbow), confidence bands (calibration table: score bin → match rate), review queue sizing (capacity formula, overflow risk), ThresholdTuner PySpark pattern (confusion_matrix, pr_at, pr_curve, calibration_table, queue_size methods), interactive threshold tuner widget.

**Key calibration insight (standing note):** For website module creation, treat budget as **unlimited** — max energy, max time, maximum detail. These are permanent artifacts and should be as comprehensive as possible. Do not ask for budget/energy on website module sessions.

**What worked:** 10-stage flow matching Module 04's structure. Table-driven teaching throughout. "X is a red herring; Y decides" contrast used for: (1) score height vs calibrated match rate, (2) "label everything later" vs label small stratified sample now. Analogies: exam grade boundary (thresholds without seeing distributions), fishing net mesh (the elbow on PR curve). Every code block shows its output; wrong→right pairs show both outputs. Interactive widget with 200 simulated labeled pairs for immediate feel.

**Known gaps:** F1 score touched but not drilled. Automated threshold selection (Youden's J, F-beta optimizer) mentioned only. Active learning for label selection not covered. Multi-threshold joint optimization (grid search) mentioned but not implemented. Calibration drift monitoring (how to detect it automatically) described conceptually but no pattern shown.

**Artifact:** score-aggregation-confidence.html (interactive, matches Module 04's HTML walkthrough format with 10 stages, live threshold tuner, 8-question self-test).

**Sustain:** Website module session — treated as unlimited budget per standing note. Full 10-stage module produced in one pass.

---

## Module 07 — LSH & MinHash (2026-06-19)

**Covered (depth `use`):** The $N^2$ scaling wall (pair explosion), fuzzy blocking, Jaccard similarity (k-gram character shingling overlap metric), MinHash signatures (permutations matrix compression), LSH banding (signatures split into $b$ bands of $r$ rows, Murmur hashing bands), tuning LSH (S-curve collision probability $P = 1-(1-s^r)^b$, threshold estimate $t \approx (1/b)^{1/r}$), PySpark MinHashLSH pattern (NGram vector inputs, `approxSimilarityJoin`, Jaccard distance threshold trap), hybrid blocking candidate join (union & distinct of blocking and LSH pairs), interactive LSH tuner widget.

**What worked:** 11-stage flow matching Module 06's structure. Table-driven teaching for Jaccard sets, MinHash permutations matrix, and S-curve thresholds. "X is a red herring; Y decides" contrast used for: LSH as a replacement for scoring (candidate generation only vs final verdict). Analogies: mail cubby sorting (banding), Venn diagram overlay (Jaccard similarity). Interactive widget with responsive bands/rows sliders and dynamic SVG-drawn S-curve graph with dot intersection, combined with a 10-profile deterministic band match simulator showing direct collisions.

**Known gaps:** SimHash for Cosine distance vector embeddings, custom hash family mathematical proofs, partition skew in signature joins, multi-probe LSH query support.

**Remediation (2026-06-20):** Rajesh came back stuck on "the MinHash trick." The website module taught it correctly but at `use` depth via the probability framing ($P=|A\cap B|/|A\cup B|$, signature tables) — when re-probed cold he said "seems very tough" and wouldn't attempt the probe. **The probability notation was the blocker, not the idea.** Fix that worked immediately: dropped all math, used a **raffle/sticker-bag analogy** — two kids each with a sticker list, dump all stickers in a bag, pull in random order, each picks "first sticker pulled that's on my list," they match only when the first-pulled sticker is on BOTH lists. He nailed the deciding-rule probe first try ("different pick because pizza is owned only by Ana not Ben") and then the quantitative gate (1-of-5 shared → ~20 of 100 matches). **Calibration lesson: for MinHash/LSH-style probabilistic concepts, lead with the concrete physical analogy and let the fraction *emerge* from it; introduce the formula only after the intuition holds. Front-loading $P(...)$ notation triggers an "this is too tough" shutdown before he engages.** Consistent with the existing note that worked-table/analogy beats formal-first for him.

**Flow rebuild (2026-06-21):** Rajesh re-opened the *published* module and rejected it hard ("module is worstly written… do you want to scare me away"). Root cause was bigger than formula-ordering: the 2026-06-20 calibration lesson had been written into the profile and promoted into SKILL.md, but **the live HTML was never patched** — it still shipped the formula-first MinHash/banding stages that caused the original disengagement. Deeper diagnosis on probing: the module read as a **catalog, not a path** — every stage a cold start with mechanism-noun headers ("The Banding Mechanism", "The Collision Probability Formula") and `sec-sub`s that restated their own titles, forcing him to self-motivate each transition. He named the real ask: *"structured so I achieve flow state and keep going, without overwhelming me,"* and *"number of stages should depend on the subject, not your template."* Cross-module audit confirmed lsh-minhash was one of the WORST offenders (0 question-headers) while `match-scoring-classification.html` and `score-aggregation-confidence.html` already did tension-handoff flow well — so the chassis was fine, the writing wasn't. **Fix shipped:** rebuilt Stages 00–05 as one descending thread — single spine question up top, one chained sticker-bag analogy world carried Jaccard → MinHash → banding → S-curve, every stage boundary a tension-handoff ("the gap this leaves open →"), all math stages analogy-first with the formula as restatement. Also fixed a genuinely *broken* MinHash worked-example/trace (self-contradictory numbers) with a hand-verified 5-shuffle illustration (Ana {A,B,C,D} / Ben {A,B,C,E}, Jaccard 3/5, agreement-rate emerges as 0.6) and corrected stale S-curve table values (A@0.70 0.970→0.975, A@0.50 0.474→0.470, B@0.90 0.490→0.477; all re-verified). **Calibration lessons promoted to SKILL.md Stage 7:** through-line gate, flow-over-coverage success criterion, subject-driven sizing (both directions), and a back-apply rule (remediation lessons must patch the published HTML, not just profile/skill). See [[feedback_modules-for-flow-state]].

**Artifact:** lsh-minhash.html (interactive; Stages 00–05 rebuilt 2026-06-21 for single-thread flow; Stages 06–09 PySpark/hybrid/tuner/self-test retained).

**Sustain:** Website module session — treated as unlimited budget. Full 11-stage module produced; flow rebuild was a Stage 7 re-execution, not a new learning session (concept already calibrated on 2026-06-20).

---

## Module 08 — Graph Theory & Clustering (2026-06-19)

**Covered (depth `use`):** Graph model (records as nodes, match verdicts as edges, connected components as master entities), BFS/DFS component-finding traced step-by-step, union-find (find/union, path compression) traced on the identical graph for direct comparison, transitive closure as the climax (moved-person scenario, bridge-edge risk where one weak match fuses two large clusters), decision criteria for BFS/DFS vs hand-rolled union-find vs GraphFrames, GraphFrames `connectedComponents()` PySpark pattern with the checkpoint-dir trap, join-back into Module 03's golden-record window functions, interactive canvas-based union-find cluster builder.

**What worked:** 10-stage flow matching Module 07's structure. "X is a red herring; Y decides" contrast used for: clustering as pairwise bookkeeping vs clustering as transitive-closure fact-creation. Same worked example (6-record graph) traced through both BFS/DFS and union-find so the two algorithms could be compared directly rather than taught in isolation. Decision-tree format (reused from Module 01's nested-types pattern) for the BFS/DFS vs union-find vs GraphFrames choice. Interactive widget used a live canvas + union-find instead of sliders, since the tunable parameter here is "which edges exist," not a numeric threshold — matched to the topic instead of reusing the slider pattern by default.

**Known gaps:** Weighted/probabilistic clustering (edge-confidence-aware community detection) not covered. Cluster-splitting/review-queue tooling at the cluster level named but not built. GraphFrames' internal algorithm (Hash-to-Min label propagation) and convergence behavior not traced. Graph algorithms beyond connectivity (centrality, shortest-path, fraud-ring community detection) out of scope.

**Flow rebuild + §14/§15 expansion (2026-06-21):** Rajesh was reading Module 07 and, before reaching Module 08, asked to pre-emptively rebuild 08 in the same single-thread flow style as the 07 rebuild AND fold in the two parked topics §14 (Entity ID Generation) and §15 (Secondary/Household Clustering). The original 08 was the pre-rebuild **catalog style** (mechanism-noun headers like "The Graph Model — Nodes, Edges, Components", "Union-Find — Merging Sets Incrementally"; `sec-sub`s restating titles; each stage cold-starting) — same latent defect as pre-rebuild 07, just never re-probed because he hadn't reached it. **Rebuilt as one descending thread:** single spine question up top, one **town-census analogy world** (forms → residents → name badges → households) carried end-to-end, every header a tension/question, every stage boundary an explicit tension-handoff. §14 and §15 added as Stages 07–08 (the natural next pulls after connected components), both **analogy-first** (badge-that-survives-the-census for deterministic hashing; people-under-one-roof for household clustering), formula/hash second — same rule that fixed 07. Grew 10→11 stages (subject-driven sizing). New hashing idioms (`collect_list`/`array_sort`/`concat_ws`/`sha2`, self-join alias, `explode`) logged in `code-fluency.md` with subgoal `.note`s. Hash digests shown clearly illustrative (never asserted a fabricated exact value). HTML self-check caught 12 callouts mis-closed with `</p>` instead of `</div>` — fixed; div/`<p>` balance verified.

**Key calibration insight (standing note, promoted) — don't ask Rajesh which sections are "required" on a domain he's learning.** He said plainly: *"I don't know anything about these things. I wouldn't complain even if it is very elaborate… you don't have to ask me if something is required or not required. Just make sure I understand everything well without getting stuck in the middle and get overwhelmed. I just want to go in a good flow state and understand stuff well."* So on website-module builds in an unfamiliar domain: **skip the optional-menu confirm question entirely** — he can't judge scope he hasn't learned, and being asked is friction. Build elaborate and complete; the only bar is flow (no stuck points, no overwhelm), not brevity. This extends the existing unlimited-budget standing note: unlimited *and* self-directed on scope. The through-line gate / pre-build check still runs — just internally, not as a question to him. See [[feedback_modules-for-flow-state]].

**Artifact:** graph-clustering.html (interactive; 2026-06-21 flow rebuild, 11 stages, town-census thread, live canvas cluster builder, 13-question self-test covering §14/§15).

**Sustain:** Website module session — treated as unlimited budget. Full 11-stage rebuild produced in one pass; this was a Stage 7 re-execution + content expansion, not a fresh learning session (clustering already calibrated 2026-06-19).

---

## Module 10 — Spark Performance (Remaining Levers) (2026-06-19)
> *(Renumbered: was Module 08. Its code-fluency drills are now the standalone **Module 09 — Code Companion**, `spark-performance-code.html`.)*

**Covered (depth `use`):** The five "remaining" perf levers (vs Module 00's structural shuffle/join/skew) — column pruning (columnar Parquet skips unread column-runs; CSV can't), predicate pushdown (filter-in-engine vs pushed-into-scan, partition pruning vs row-group min/max, `.explain()`/`PushedFilters` verification), partition sizing (~128 MB target, `repartition` full-shuffle/can-grow/even vs `coalesce` no-shuffle/shrink-only/uneven, the 200-default `shuffle.partitions` tiny-files trap), spill & memory (execution-region overflow→disk, spill≠OOM, the "add more RAM" red herring → it's per-task data-vs-memory, fix with more/smaller partitions or skew), caching (reuse-across-branches win, "cache everything" anti-pattern, cache-is-lazy), AQE (coalesce/skew-join/dynamic-broadcast runtime re-optimization; "AQE owns the shuffle, you own the read"), the symptom→lever decision tree, full tuned-pipeline PySpark pattern, combined two-mode tuning-calculator widget.

**Code Companion — now a standalone before-module (Module 09):** `spark-performance-code.html` drills the PySpark idioms separately from this concept walkthrough — predict-then-reveal, fresh data, two reps per idiom, hand-verified trace values, violet accent. Originally shipped as a supplement; **later promoted to its own module (09), placed immediately before this one (10)**, alongside the sibling **Module 05 — Code Companion** (`score-aggregation-confidence-code.html`) for ThresholdTuner. Decimal stage labels (the old "Stage 08.1") were dropped in the promotion. Pattern: the concept module's PySpark-pattern stage shows each idiom once and links to the preceding companion module; the companion gives the extra reps. Idioms drilled here: `.select` pruning, `repartition` vs `coalesce`, `F.broadcast`, `.cache` laziness, `spark.sql.shuffle.partitions`. Logged in `code-fluency.md`.

**What worked (held this session):** 11-stage flow matching Module 08. Table-driven throughout (cost-trap table, row-major-vs-columnar, repartition-vs-coalesce, pushdown-mechanism, AQE-handles-vs-you). "X is a red herring; Y decides" used twice — (1) "add more RAM" vs data-per-task for spill, (2) "cache everything" vs reuse-pattern. Desks/paper analogy reused from Module 00 for partition sizing (continuity across the track). Widget matched to the topic's real tunables (partition sizing + shuffle count) rather than reusing a default slider pattern blindly. Every code block shows highlighted lever lines with inline `why` comments.

**Known gaps:** Bucketing/pre-sorted tables, unified-memory-model knobs in detail (`spark.memory.fraction`/`storageFraction`), `.persist()` storage levels, hands-on Spark-UI tour, dynamic resource allocation/cluster sizing. (Carried in roadmap "Parked / for later".)

**Artifact:** spark-performance.html (11 stages, combined partition/shuffle tuning calculator, 8-question self-test) + spark-performance-code.html (Code Companion, 5 idioms × 2 predict-then-reveal drills).

**Sustain:** Website module session — treated as unlimited budget per standing note. Full 11-stage module + companion produced in one pass.
