# Rajesh's Learning Profile

Generated by learning-framework skill. Read by Stages 1 and 2; written by Stage 6.
Hand-editable.

Last updated: 2026-06-19

---

## Meta-notes on the framework itself (from v1's first run)

- 4-loop cadence felt right for "moderate complexity, standard budget" sessions
- Running calibration interval of K=3 was correct here — calibrated after Loop 3 just before synthesis
- The artifact-spec-drives-everything-backward principle held: declaring "3 day-templates" in Stage 1 was the right call initially, and the mid-session renegotiation to "skeleton-with-modules" was a real adaptation to his actual posture, not just a label change

---

## Domain: spark / data-engineering (entity resolution)

**Chunk size:** small–medium — one mechanism per loop (shuffle / joins / skew). Do NOT batch multiple strategies into one loop; each sub-concept needs its own worked treatment.

**Sustain window:** short and spiky. ~45-min budget declared but real energy oscillated: 3 → 3 → crashed to 1 after ~30 min → recovered to 3 after a pause → completed Loop 3. Honor energy-1 stops immediately (banked correctly, did not push into skew on the empty tank; he came back recharged). Expect a dip around the 30-min mark; a clean stop-offer at that point works well.

**Angles that worked:**
- ✅ **Worked tables, traced row-by-row** — by far the biggest lever. He explicitly asked twice for "take some tables and show me." Always draw input table → scatter across desks → shuffle → result. Tables > prose, every time.
- ✅ **Office/desks analogy** — partition=desk, worker=person, shuffle=carrying forms across the room. Landed instantly and he reused the vocabulary correctly in his own answers.
- ✅ **Mechanism-first, then the rule** — derive the rule from the worked example rather than asserting it.
- ✅ **Growth tables** (rows vs pairs) to make quadratic explosion visceral — the N²/2 table did the H/G work in one glance.

**Angles that didn't work / not tested:**
- ❌ **Terse multi-item intros** — compressing 3 join strategies into ~4 sentences each was too thin; he called it out directly. Under-teaching is his more common failure mode than over-teaching. (Now encoded in SKILL.md Stage 3b.)

**Prerequisites confirmed:**
- Strong on his own MDM/PySpark ER pipeline as anchor (PipelineEngine, StandardizationPlugin, blocking, candidate pairs).
- Solid: narrow vs shuffle, self-join condition, broadcast vs sort-merge.
- Gap exposed: had NOT seen self-joins before this session (got there fast once shown).
- Pattern: gets concepts right but drops the *labels* (named "skew" as its fix "better blocking key"; knew the recall trade-off but not the term "false negative"). At `use` depth, attach vocabulary explicitly after the concept lands.

**Known gaps (2026-06-16):**
- Skew fixes salting + isolate-hot-key covered at recognize-only, not drilled.
- Window functions, complex/nested types (#2), LSH/MinHash (#5), graph/connected-components (#6), string normalization (#3), perf (#7), pipeline design (#8) — all still ahead in this 8-topic arc.

**Sessions:**
- 2026-06-16 — Source: AI knowledge + pipeline context — Loops: shuffle → joins → skew (his H/G). Artifact: shuffle/join/skew decision-card. Depth `use`, passed (needed labels, not concept rebuilds). Note: he declined the Notion learning-log update this session.

**Notes:**
- 8-topic Spark/ER list is a 4–6 session arc. Entity Resolution (#4) is the declared spine; he chose bottom-up (mechanics first). Resume next session at: **complex/nested types (#2)** or **LSH (#5)**, OR drill salting if he wants skew-fixes hands-on.
- Bottom-up sequencing worked well — each mechanic paid off visibly into the ER problem he cares about.

---

## Module 01 — Complex types & nested data (2026-06-18)

**Covered (depth `use`):** Array (explode vs explode_outer, size/array_contains/filter, null-drop gotcha), Struct (dot access, struct() rebuild, fixed-fields contract), Map (key access, map_keys/values, explode-on-map, boolean value types, schema-evolution-free). Decision tree (Q1 fixed parts→struct, Q2 variable same-type→array, Q3 open-ended keys→map).

**Probe performance:** Strong. ~12/14 first-pass correct across loops. Syntax slips (withColumn first-arg must be plain string not col(); substring is 3-arg 1-indexed) but concepts solid. The two borderline scenarios (geo coord, boolean flags) initially wobbled then self-corrected.

**Key calibration insight:** He talks himself OUT of correct answers on borderline cases by over-weighting a salient-but-irrelevant feature (chose map for lat/lng because "both values are double"). Fix that worked: an explicit contrast table showing two cases with the SAME surface feature landing on different types, proving the feature isn't the deciding factor. The "X is a red herring; Y decides" framing lands well — use it whenever he's pattern-matching on the wrong signal.

**What worked:** Scenario-recognition drills (he explicitly requested "use cases to relate and absorb"). Real MDM data shapes (phones/address/signals) as the running examples. Decision tree as an ordered ask (Q1→Q2→Q3), not a flat list. Side-by-side same-feature-different-answer tables. Coding syntax is still weak — he reads/adapts from examples but can't yet type from scratch ("not yet able to type"); keep showing runnable snippets, don't assume recall.

**Known gaps:** null-handling inside arrays (Loop 5), schema evolution mechanics (Loop 6), array-of-structs (the real MDM shape — touched not drilled), transform/inline. These roll into a short follow-up before Module 02.

**Artifact:** nested-types-decision-card.html (interactive, matches Module 00's HTML walkthrough format).

**Sustain:** Solid full module across loops 1–4 at energy 3, no crash this time. Scenario-drilling re-energized him near the end rather than draining — recognition practice is a good "second wind" activity for him.

---

## Module 05 — Score Aggregation & Confidence (2026-06-19)

**Covered (depth `use`):** Why fixed thresholds fail (distribution problem), labeled data (stratified sampling, sizing), confusion matrix (TP/FP/FN/TN), precision ("how clean are merges") and recall ("how many matches caught"), PR curve (sweep + elbow), confidence bands (calibration table: score bin → match rate), review queue sizing (capacity formula, overflow risk), ThresholdTuner PySpark pattern (confusion_matrix, pr_at, pr_curve, calibration_table, queue_size methods), interactive threshold tuner widget.

**Key calibration insight (standing note):** For website module creation, treat budget as **unlimited** — max energy, max time, maximum detail. These are permanent artifacts and should be as comprehensive as possible. Do not ask for budget/energy on website module sessions.

**What worked:** 10-stage flow matching Module 04's structure. Table-driven teaching throughout. "X is a red herring; Y decides" contrast used for: (1) score height vs calibrated match rate, (2) "label everything later" vs label small stratified sample now. Analogies: exam grade boundary (thresholds without seeing distributions), fishing net mesh (the elbow on PR curve). Every code block shows its output; wrong→right pairs show both outputs. Interactive widget with 200 simulated labeled pairs for immediate feel.

**Known gaps:** F1 score touched but not drilled. Automated threshold selection (Youden's J, F-beta optimizer) mentioned only. Active learning for label selection not covered. Multi-threshold joint optimization (grid search) mentioned but not implemented. Calibration drift monitoring (how to detect it automatically) described conceptually but no pattern shown.

**Artifact:** score-aggregation-confidence.html (interactive, matches Module 04's HTML walkthrough format with 10 stages, live threshold tuner, 8-question self-test).

**Sustain:** Website module session — treated as unlimited budget per standing note. Full 10-stage module produced in one pass.
